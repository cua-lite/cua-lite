# Qwen3.5 desktop.use — native screenshot, ONE of history, native <think> reasoning.
#
# The reasoning arm of the campaign's high-res profile. Pairs with
# desktop.use.highr.h1.yaml, which it differs from by enable_thinking alone.
#
# Reasoning teachers only. gpt5_5's published rows carry the Thought in
# reasoning_content (devs/data/internalize_cot.py runs before staging);
# qwen3_5_27b's carry the native <think> straight from collection. qwen3_8_27b
# rows have none, so this config would train an empty <think> block on them.
#
#   desktop.use.default.yaml             native 1920x1080  <=4 images  Action-only
#   desktop.use.default.reasoning.yaml   native 1920x1080  <=4 images  <think>
#   desktop.use.lowr.h4.yaml             1280x720          <=4 images  Action-only
#   desktop.use.lowr.h4.reasoning.yaml   1280x720          <=4 images  <think>
#   desktop.use.highr.h1.yaml            native 1920x1080    1 image   Action-only
#   desktop.use.highr.h1.reasoning.yaml  native 1920x1080    1 image   <think>      <- this file
#
# No env_id: rollout picks the env via --env-id, export_sft picks the adapter per row
# from metadata.dims. system_prompt omitted -> the adapter's canonical USE_SYSTEM_PROMPT.

agent_id: "qwen3_5"
agent_kwargs:
  sampling_kwargs:  # rollout.py only; export_sft drops it, RL sets sampling in the trainer
    temperature: 0.0
  enable_thinking: true     # native <think> channel (no prompted Thought:/Action: format)
  # resolution omitted -> native 1920x1080; smart_resize rounds to 1920x1088.
  protocol_kwargs:
    history_n: 1  # one turn kept -> one screenshot; image_max/fold_size stay at 4/4

env_kwargs:
  max_steps: 30
  loop_detect: 5
  # Required, not cosmetic: without it the env rejects both as inactive extras and the
  # model can never end an episode. Qwen's native `answer` lowers to `response`.
  extra_tools: ["response", "terminate"]
